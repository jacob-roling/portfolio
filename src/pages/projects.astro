---
import Base from "#layouts/Base.astro";
import ProjectsList from "#components/ProjectsList.astro";
import { sortedAndFilteredArticles } from "#utils";

const articles = await sortedAndFilteredArticles();
const projects = articles.filter(({ data }) => data.tags.includes("Project"));
---

<Base>
  <Fragment>
    <title>Projects — Jacob Roling</title>
    <meta
      name="description"
      content="A portfolio of the projects I have done over the years."
    />
  </Fragment>
  <main id="main" class="container typography pt-header py-4">
    <h1 id="projects">Projects</h1>
    <ProjectsList {projects} />
  </main>
</Base>
